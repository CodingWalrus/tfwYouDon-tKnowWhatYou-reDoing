# This imports the app variable from the app package

from app import app
# The Flask project can be activated by running "set FLASK_APP=microblog.py" in the command prompt
# and then entering "flask run"